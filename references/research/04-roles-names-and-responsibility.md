# 角色、正名与责任 / Roles, Names, and Responsibility

Role ethics, rectification of names, responsibility, and the structure of cooperation.

## Why This File Exists

- This file supports the Confucius person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 孔子 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is Role ethics, rectification of names, responsibility, and the structure of cooperation..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### rectification of names — Definition

- What this names: within `Role ethics, rectification of names, responsibility, and the structure of cooperation`, `rectification of names` points to a concrete node rather than a prestige label.
- Object boundary: treat `rectification of names` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `rectification of names` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `rectification of names` before pulling it into a modern analogy.
- Skill value: `rectification of names` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `rectification of names` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `rectification of names` clearly enough that a later answer can say what `rectification of names` includes and excludes.

### rectification of names — Historical Context

- Historical location: ask when `rectification of names` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `rectification of names` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `rectification of names` says, but why `rectification of names` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `rectification of names` from later packaging or selective reuse.
- Reading move: map `rectification of names` against the timeline before treating `rectification of names` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `rectification of names` before extracting any lesson from `rectification of names`.
- Failure risk: without context, `rectification of names` can look cleaner, more universal, and more innocent than it actually was.

### rectification of names — Internal Tension

- Main tension: `rectification of names` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `rectification of names` makes possible and what `rectification of names` quietly suppresses at the same time.
- Interpretive value: the tension in `rectification of names` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `rectification of names` double-sided rather than converting `rectification of names` into moral reassurance.
- Research warning: if every sentence about `rectification of names` sounds admiring or dismissive, `rectification of names` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `rectification of names`.
- Better question: what does `rectification of names` solve, and what new silence does `rectification of names` create?

### rectification of names — Key Distinction

- Core distinction: separate `rectification of names` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `rectification of names`.
- Typical confusion: readers often mistake `rectification of names` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `rectification of names` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `rectification of names`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `rectification of names` by accident.
- Operational cue: define what `rectification of names` is not before claiming to know what `rectification of names` is.

### rectification of names — Method

- Method question: how does `rectification of names` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `rectification of names` rather than inspirational phrasing.
- Research use: `rectification of names` matters when `rectification of names` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `rectification of names`, not costume around `rectification of names`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `rectification of names`.
- Modern move: only transfer the mechanism in `rectification of names` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `rectification of names` without reproducing the method inside `rectification of names`.

### rectification of names — Operational Use

- Operational role: `rectification of names` belongs in the skill because `rectification of names` changes how prompts are framed and how problems are cut.
- Decision value: `rectification of names` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `rectification of names` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `rectification of names` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `rectification of names` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `rectification of names` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `rectification of names` make me check before I act?

### rectification of names — Transfer To Modern Problems

- Transfer principle: bring `rectification of names` forward only when the structural analogy around `rectification of names` is stronger than the historical costume.
- Modern fit: `rectification of names` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `rectification of names` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `rectification of names`, not the surrounding mythology around `rectification of names`.
- Bad transfer: using `rectification of names` to justify positions `rectification of names` was never built to adjudicate.
- Practical test: if the analogy for `rectification of names` cannot survive plainer language, `rectification of names` probably does not belong in the answer.
- Design implication: every modern use of `rectification of names` should make the analogy explicit instead of pretending `rectification of names` is self-evident.

### rectification of names — Failure Mode

- Failure pattern: `rectification of names` goes bad when `rectification of names` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `rectification of names` for authority, mood, or aesthetic and lose the underlying discipline in `rectification of names`.
- Research signal: when `rectification of names` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `rectification of names`.
- User risk: answers can start sounding impressive while `rectification of names` becomes less falsifiable.
- Repository risk: `rectification of names` can silently drift from operating concept into mascot.
- Correction move: pull `rectification of names` back to evidence, mechanism, and concrete limits.
- Best warning label: `rectification of names` should increase rigor, not lower the burden of proof.

### rectification of names — Common Misreading

- Typical misreading: `rectification of names` is often taken as a broad worldview when `rectification of names` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `rectification of names`.
- Consequence: readers then import claims into `rectification of names` that the corpus does not actually support for `rectification of names`.
- Skill response: keep `rectification of names` attached to the texts, cases, and institutional situations that make `rectification of names` legible.
- Modern correction: show what `rectification of names` can illuminate without pretending `rectification of names` answers everything nearby.
- Language fix: name the overreach around `rectification of names` directly instead of just gesturing at nuance.
- Reader test: if `rectification of names` suddenly explains every problem, `rectification of names` is being misread.

### rectification of names — Boundary Condition

- Boundary condition: `rectification of names` only travels well when the receiving problem shares enough structure with the original one behind `rectification of names`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `rectification of names`.
- Ethical test: some uses of `rectification of names` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `rectification of names` into careless advice.
- Good limit: it is fine for `rectification of names` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `rectification of names` has no boundary because `rectification of names` feels generically wise.
- Operational question: under what conditions should `rectification of names` be left unused?

### rectification of names — Better Question

- Better question: instead of asking whether `rectification of names` is simply true, ask what problem `rectification of names` was trying to solve.
- Reader upgrade: this turns `rectification of names` from a label into a live diagnostic instrument.
- Research upgrade: `rectification of names` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `rectification of names` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `rectification of names` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `rectification of names` becomes useful when `rectification of names` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `rectification of names` is framed as a problem-solving move?

### rectification of names — Example Move

- Example move: use `rectification of names` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `rectification of names` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `rectification of names` is strongest when `rectification of names` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `rectification of names` lets the user do that they could not do before.
- Transfer discipline: keep the move around `rectification of names` small and explicit so the analogy does not sprawl.
- Repository standard: if `rectification of names` never produces a concrete move, `rectification of names` is not yet operational.
- User question: what is one thing `rectification of names` would make me inspect differently right now?

### rectification of names — What To Preserve

- Preserve the pressure: `rectification of names` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `rectification of names` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `rectification of names`.
- Preserve the routing value: `rectification of names` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `rectification of names` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `rectification of names` stops is part of preserving `rectification of names` well.
- Preserve the usefulness: a transferred `rectification of names` should still help someone make a cleaner judgment.

### rectification of names — What To Drop

- Drop the costume: `rectification of names` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `rectification of names` has been understood.
- Drop the universal claim: `rectification of names` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `rectification of names` survives only as a catchphrase, `rectification of names` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `rectification of names` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `rectification of names` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `rectification of names` and remove the surrounding noise.

### rectification of names — Design Implication

- Design implication: `rectification of names` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `rectification of names` that later edits can stay honest.
- UX implication: users should feel `rectification of names` changing the angle of analysis, not just the tone.
- Evaluation implication: test `rectification of names` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `rectification of names` only if they sharpen the boundary of `rectification of names` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `rectification of names` should refuse.
- Bottom line: `rectification of names` belongs in the repo only if `rectification of names` keeps improving judgment after the historical costume is removed.

### role and duty — Definition

- What this names: within `Role ethics, rectification of names, responsibility, and the structure of cooperation`, `role and duty` points to a concrete node rather than a prestige label.
- Object boundary: treat `role and duty` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `role and duty` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `role and duty` before pulling it into a modern analogy.
- Skill value: `role and duty` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `role and duty` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `role and duty` clearly enough that a later answer can say what `role and duty` includes and excludes.

### role and duty — Historical Context

- Historical location: ask when `role and duty` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `role and duty` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `role and duty` says, but why `role and duty` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `role and duty` from later packaging or selective reuse.
- Reading move: map `role and duty` against the timeline before treating `role and duty` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `role and duty` before extracting any lesson from `role and duty`.
- Failure risk: without context, `role and duty` can look cleaner, more universal, and more innocent than it actually was.

### role and duty — Internal Tension

- Main tension: `role and duty` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `role and duty` makes possible and what `role and duty` quietly suppresses at the same time.
- Interpretive value: the tension in `role and duty` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `role and duty` double-sided rather than converting `role and duty` into moral reassurance.
- Research warning: if every sentence about `role and duty` sounds admiring or dismissive, `role and duty` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `role and duty`.
- Better question: what does `role and duty` solve, and what new silence does `role and duty` create?

### role and duty — Key Distinction

- Core distinction: separate `role and duty` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `role and duty`.
- Typical confusion: readers often mistake `role and duty` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `role and duty` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `role and duty`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `role and duty` by accident.
- Operational cue: define what `role and duty` is not before claiming to know what `role and duty` is.

### role and duty — Method

- Method question: how does `role and duty` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `role and duty` rather than inspirational phrasing.
- Research use: `role and duty` matters when `role and duty` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `role and duty`, not costume around `role and duty`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `role and duty`.
- Modern move: only transfer the mechanism in `role and duty` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `role and duty` without reproducing the method inside `role and duty`.

### role and duty — Operational Use

- Operational role: `role and duty` belongs in the skill because `role and duty` changes how prompts are framed and how problems are cut.
- Decision value: `role and duty` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `role and duty` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `role and duty` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `role and duty` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `role and duty` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `role and duty` make me check before I act?

### role and duty — Transfer To Modern Problems

- Transfer principle: bring `role and duty` forward only when the structural analogy around `role and duty` is stronger than the historical costume.
- Modern fit: `role and duty` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `role and duty` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `role and duty`, not the surrounding mythology around `role and duty`.
- Bad transfer: using `role and duty` to justify positions `role and duty` was never built to adjudicate.
- Practical test: if the analogy for `role and duty` cannot survive plainer language, `role and duty` probably does not belong in the answer.
- Design implication: every modern use of `role and duty` should make the analogy explicit instead of pretending `role and duty` is self-evident.

### role and duty — Failure Mode

- Failure pattern: `role and duty` goes bad when `role and duty` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `role and duty` for authority, mood, or aesthetic and lose the underlying discipline in `role and duty`.
- Research signal: when `role and duty` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `role and duty`.
- User risk: answers can start sounding impressive while `role and duty` becomes less falsifiable.
- Repository risk: `role and duty` can silently drift from operating concept into mascot.
- Correction move: pull `role and duty` back to evidence, mechanism, and concrete limits.
- Best warning label: `role and duty` should increase rigor, not lower the burden of proof.

### role and duty — Common Misreading

- Typical misreading: `role and duty` is often taken as a broad worldview when `role and duty` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `role and duty`.
- Consequence: readers then import claims into `role and duty` that the corpus does not actually support for `role and duty`.
- Skill response: keep `role and duty` attached to the texts, cases, and institutional situations that make `role and duty` legible.
- Modern correction: show what `role and duty` can illuminate without pretending `role and duty` answers everything nearby.
- Language fix: name the overreach around `role and duty` directly instead of just gesturing at nuance.
- Reader test: if `role and duty` suddenly explains every problem, `role and duty` is being misread.

### role and duty — Boundary Condition

- Boundary condition: `role and duty` only travels well when the receiving problem shares enough structure with the original one behind `role and duty`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `role and duty`.
- Ethical test: some uses of `role and duty` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `role and duty` into careless advice.
- Good limit: it is fine for `role and duty` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `role and duty` has no boundary because `role and duty` feels generically wise.
- Operational question: under what conditions should `role and duty` be left unused?

### role and duty — Better Question

- Better question: instead of asking whether `role and duty` is simply true, ask what problem `role and duty` was trying to solve.
- Reader upgrade: this turns `role and duty` from a label into a live diagnostic instrument.
- Research upgrade: `role and duty` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `role and duty` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `role and duty` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `role and duty` becomes useful when `role and duty` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `role and duty` is framed as a problem-solving move?

### role and duty — Example Move

- Example move: use `role and duty` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `role and duty` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `role and duty` is strongest when `role and duty` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `role and duty` lets the user do that they could not do before.
- Transfer discipline: keep the move around `role and duty` small and explicit so the analogy does not sprawl.
- Repository standard: if `role and duty` never produces a concrete move, `role and duty` is not yet operational.
- User question: what is one thing `role and duty` would make me inspect differently right now?

### role and duty — What To Preserve

- Preserve the pressure: `role and duty` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `role and duty` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `role and duty`.
- Preserve the routing value: `role and duty` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `role and duty` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `role and duty` stops is part of preserving `role and duty` well.
- Preserve the usefulness: a transferred `role and duty` should still help someone make a cleaner judgment.

### role and duty — What To Drop

- Drop the costume: `role and duty` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `role and duty` has been understood.
- Drop the universal claim: `role and duty` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `role and duty` survives only as a catchphrase, `role and duty` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `role and duty` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `role and duty` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `role and duty` and remove the surrounding noise.

### role and duty — Design Implication

- Design implication: `role and duty` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `role and duty` that later edits can stay honest.
- UX implication: users should feel `role and duty` changing the angle of analysis, not just the tone.
- Evaluation implication: test `role and duty` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `role and duty` only if they sharpen the boundary of `role and duty` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `role and duty` should refuse.
- Bottom line: `role and duty` belongs in the repo only if `role and duty` keeps improving judgment after the historical costume is removed.

### filial and civic responsibility — Definition

- What this names: within `Role ethics, rectification of names, responsibility, and the structure of cooperation`, `filial and civic responsibility` points to a concrete node rather than a prestige label.
- Object boundary: treat `filial and civic responsibility` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `filial and civic responsibility` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `filial and civic responsibility` before pulling it into a modern analogy.
- Skill value: `filial and civic responsibility` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `filial and civic responsibility` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `filial and civic responsibility` clearly enough that a later answer can say what `filial and civic responsibility` includes and excludes.

### filial and civic responsibility — Historical Context

- Historical location: ask when `filial and civic responsibility` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `filial and civic responsibility` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `filial and civic responsibility` says, but why `filial and civic responsibility` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `filial and civic responsibility` from later packaging or selective reuse.
- Reading move: map `filial and civic responsibility` against the timeline before treating `filial and civic responsibility` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `filial and civic responsibility` before extracting any lesson from `filial and civic responsibility`.
- Failure risk: without context, `filial and civic responsibility` can look cleaner, more universal, and more innocent than it actually was.

### filial and civic responsibility — Internal Tension

- Main tension: `filial and civic responsibility` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `filial and civic responsibility` makes possible and what `filial and civic responsibility` quietly suppresses at the same time.
- Interpretive value: the tension in `filial and civic responsibility` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `filial and civic responsibility` double-sided rather than converting `filial and civic responsibility` into moral reassurance.
- Research warning: if every sentence about `filial and civic responsibility` sounds admiring or dismissive, `filial and civic responsibility` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `filial and civic responsibility`.
- Better question: what does `filial and civic responsibility` solve, and what new silence does `filial and civic responsibility` create?

### filial and civic responsibility — Key Distinction

- Core distinction: separate `filial and civic responsibility` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `filial and civic responsibility`.
- Typical confusion: readers often mistake `filial and civic responsibility` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `filial and civic responsibility` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `filial and civic responsibility`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `filial and civic responsibility` by accident.
- Operational cue: define what `filial and civic responsibility` is not before claiming to know what `filial and civic responsibility` is.

### filial and civic responsibility — Method

- Method question: how does `filial and civic responsibility` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `filial and civic responsibility` rather than inspirational phrasing.
- Research use: `filial and civic responsibility` matters when `filial and civic responsibility` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `filial and civic responsibility`, not costume around `filial and civic responsibility`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `filial and civic responsibility`.
- Modern move: only transfer the mechanism in `filial and civic responsibility` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `filial and civic responsibility` without reproducing the method inside `filial and civic responsibility`.

### filial and civic responsibility — Operational Use

- Operational role: `filial and civic responsibility` belongs in the skill because `filial and civic responsibility` changes how prompts are framed and how problems are cut.
- Decision value: `filial and civic responsibility` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `filial and civic responsibility` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `filial and civic responsibility` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `filial and civic responsibility` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `filial and civic responsibility` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `filial and civic responsibility` make me check before I act?

### filial and civic responsibility — Transfer To Modern Problems

- Transfer principle: bring `filial and civic responsibility` forward only when the structural analogy around `filial and civic responsibility` is stronger than the historical costume.
- Modern fit: `filial and civic responsibility` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `filial and civic responsibility` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `filial and civic responsibility`, not the surrounding mythology around `filial and civic responsibility`.
- Bad transfer: using `filial and civic responsibility` to justify positions `filial and civic responsibility` was never built to adjudicate.
- Practical test: if the analogy for `filial and civic responsibility` cannot survive plainer language, `filial and civic responsibility` probably does not belong in the answer.
- Design implication: every modern use of `filial and civic responsibility` should make the analogy explicit instead of pretending `filial and civic responsibility` is self-evident.

### filial and civic responsibility — Failure Mode

- Failure pattern: `filial and civic responsibility` goes bad when `filial and civic responsibility` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `filial and civic responsibility` for authority, mood, or aesthetic and lose the underlying discipline in `filial and civic responsibility`.
- Research signal: when `filial and civic responsibility` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `filial and civic responsibility`.
- User risk: answers can start sounding impressive while `filial and civic responsibility` becomes less falsifiable.
- Repository risk: `filial and civic responsibility` can silently drift from operating concept into mascot.
- Correction move: pull `filial and civic responsibility` back to evidence, mechanism, and concrete limits.
- Best warning label: `filial and civic responsibility` should increase rigor, not lower the burden of proof.

### filial and civic responsibility — Common Misreading

- Typical misreading: `filial and civic responsibility` is often taken as a broad worldview when `filial and civic responsibility` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `filial and civic responsibility`.
- Consequence: readers then import claims into `filial and civic responsibility` that the corpus does not actually support for `filial and civic responsibility`.
- Skill response: keep `filial and civic responsibility` attached to the texts, cases, and institutional situations that make `filial and civic responsibility` legible.
- Modern correction: show what `filial and civic responsibility` can illuminate without pretending `filial and civic responsibility` answers everything nearby.
- Language fix: name the overreach around `filial and civic responsibility` directly instead of just gesturing at nuance.
- Reader test: if `filial and civic responsibility` suddenly explains every problem, `filial and civic responsibility` is being misread.

### filial and civic responsibility — Boundary Condition

- Boundary condition: `filial and civic responsibility` only travels well when the receiving problem shares enough structure with the original one behind `filial and civic responsibility`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `filial and civic responsibility`.
- Ethical test: some uses of `filial and civic responsibility` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `filial and civic responsibility` into careless advice.
- Good limit: it is fine for `filial and civic responsibility` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `filial and civic responsibility` has no boundary because `filial and civic responsibility` feels generically wise.
- Operational question: under what conditions should `filial and civic responsibility` be left unused?

### filial and civic responsibility — Better Question

- Better question: instead of asking whether `filial and civic responsibility` is simply true, ask what problem `filial and civic responsibility` was trying to solve.
- Reader upgrade: this turns `filial and civic responsibility` from a label into a live diagnostic instrument.
- Research upgrade: `filial and civic responsibility` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `filial and civic responsibility` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `filial and civic responsibility` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `filial and civic responsibility` becomes useful when `filial and civic responsibility` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `filial and civic responsibility` is framed as a problem-solving move?

### filial and civic responsibility — Example Move

- Example move: use `filial and civic responsibility` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `filial and civic responsibility` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `filial and civic responsibility` is strongest when `filial and civic responsibility` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `filial and civic responsibility` lets the user do that they could not do before.
- Transfer discipline: keep the move around `filial and civic responsibility` small and explicit so the analogy does not sprawl.
- Repository standard: if `filial and civic responsibility` never produces a concrete move, `filial and civic responsibility` is not yet operational.
- User question: what is one thing `filial and civic responsibility` would make me inspect differently right now?

### filial and civic responsibility — What To Preserve

- Preserve the pressure: `filial and civic responsibility` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `filial and civic responsibility` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `filial and civic responsibility`.
- Preserve the routing value: `filial and civic responsibility` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `filial and civic responsibility` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `filial and civic responsibility` stops is part of preserving `filial and civic responsibility` well.
- Preserve the usefulness: a transferred `filial and civic responsibility` should still help someone make a cleaner judgment.

### filial and civic responsibility — What To Drop

- Drop the costume: `filial and civic responsibility` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `filial and civic responsibility` has been understood.
- Drop the universal claim: `filial and civic responsibility` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `filial and civic responsibility` survives only as a catchphrase, `filial and civic responsibility` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `filial and civic responsibility` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `filial and civic responsibility` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `filial and civic responsibility` and remove the surrounding noise.

### filial and civic responsibility — Design Implication

- Design implication: `filial and civic responsibility` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `filial and civic responsibility` that later edits can stay honest.
- UX implication: users should feel `filial and civic responsibility` changing the angle of analysis, not just the tone.
- Evaluation implication: test `filial and civic responsibility` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `filial and civic responsibility` only if they sharpen the boundary of `filial and civic responsibility` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `filial and civic responsibility` should refuse.
- Bottom line: `filial and civic responsibility` belongs in the repo only if `filial and civic responsibility` keeps improving judgment after the historical costume is removed.

### relationship order — Definition

- What this names: within `Role ethics, rectification of names, responsibility, and the structure of cooperation`, `relationship order` points to a concrete node rather than a prestige label.
- Object boundary: treat `relationship order` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `relationship order` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `relationship order` before pulling it into a modern analogy.
- Skill value: `relationship order` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `relationship order` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `relationship order` clearly enough that a later answer can say what `relationship order` includes and excludes.

### relationship order — Historical Context

- Historical location: ask when `relationship order` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `relationship order` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `relationship order` says, but why `relationship order` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `relationship order` from later packaging or selective reuse.
- Reading move: map `relationship order` against the timeline before treating `relationship order` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `relationship order` before extracting any lesson from `relationship order`.
- Failure risk: without context, `relationship order` can look cleaner, more universal, and more innocent than it actually was.

### relationship order — Internal Tension

- Main tension: `relationship order` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `relationship order` makes possible and what `relationship order` quietly suppresses at the same time.
- Interpretive value: the tension in `relationship order` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `relationship order` double-sided rather than converting `relationship order` into moral reassurance.
- Research warning: if every sentence about `relationship order` sounds admiring or dismissive, `relationship order` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `relationship order`.
- Better question: what does `relationship order` solve, and what new silence does `relationship order` create?

### relationship order — Key Distinction

- Core distinction: separate `relationship order` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `relationship order`.
- Typical confusion: readers often mistake `relationship order` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `relationship order` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `relationship order`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `relationship order` by accident.
- Operational cue: define what `relationship order` is not before claiming to know what `relationship order` is.

### relationship order — Method

- Method question: how does `relationship order` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `relationship order` rather than inspirational phrasing.
- Research use: `relationship order` matters when `relationship order` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `relationship order`, not costume around `relationship order`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `relationship order`.
- Modern move: only transfer the mechanism in `relationship order` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `relationship order` without reproducing the method inside `relationship order`.

### relationship order — Operational Use

- Operational role: `relationship order` belongs in the skill because `relationship order` changes how prompts are framed and how problems are cut.
- Decision value: `relationship order` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `relationship order` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `relationship order` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `relationship order` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `relationship order` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `relationship order` make me check before I act?

### relationship order — Transfer To Modern Problems

- Transfer principle: bring `relationship order` forward only when the structural analogy around `relationship order` is stronger than the historical costume.
- Modern fit: `relationship order` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `relationship order` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `relationship order`, not the surrounding mythology around `relationship order`.
- Bad transfer: using `relationship order` to justify positions `relationship order` was never built to adjudicate.
- Practical test: if the analogy for `relationship order` cannot survive plainer language, `relationship order` probably does not belong in the answer.
- Design implication: every modern use of `relationship order` should make the analogy explicit instead of pretending `relationship order` is self-evident.

### relationship order — Failure Mode

- Failure pattern: `relationship order` goes bad when `relationship order` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `relationship order` for authority, mood, or aesthetic and lose the underlying discipline in `relationship order`.
- Research signal: when `relationship order` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `relationship order`.
- User risk: answers can start sounding impressive while `relationship order` becomes less falsifiable.
- Repository risk: `relationship order` can silently drift from operating concept into mascot.
- Correction move: pull `relationship order` back to evidence, mechanism, and concrete limits.
- Best warning label: `relationship order` should increase rigor, not lower the burden of proof.

### relationship order — Common Misreading

- Typical misreading: `relationship order` is often taken as a broad worldview when `relationship order` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `relationship order`.
- Consequence: readers then import claims into `relationship order` that the corpus does not actually support for `relationship order`.
- Skill response: keep `relationship order` attached to the texts, cases, and institutional situations that make `relationship order` legible.
- Modern correction: show what `relationship order` can illuminate without pretending `relationship order` answers everything nearby.
- Language fix: name the overreach around `relationship order` directly instead of just gesturing at nuance.
- Reader test: if `relationship order` suddenly explains every problem, `relationship order` is being misread.

### relationship order — Boundary Condition

- Boundary condition: `relationship order` only travels well when the receiving problem shares enough structure with the original one behind `relationship order`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `relationship order`.
- Ethical test: some uses of `relationship order` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `relationship order` into careless advice.
- Good limit: it is fine for `relationship order` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `relationship order` has no boundary because `relationship order` feels generically wise.
- Operational question: under what conditions should `relationship order` be left unused?

### relationship order — Better Question

- Better question: instead of asking whether `relationship order` is simply true, ask what problem `relationship order` was trying to solve.
- Reader upgrade: this turns `relationship order` from a label into a live diagnostic instrument.
- Research upgrade: `relationship order` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `relationship order` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `relationship order` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `relationship order` becomes useful when `relationship order` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `relationship order` is framed as a problem-solving move?

### relationship order — Example Move

- Example move: use `relationship order` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `relationship order` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `relationship order` is strongest when `relationship order` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `relationship order` lets the user do that they could not do before.
- Transfer discipline: keep the move around `relationship order` small and explicit so the analogy does not sprawl.
- Repository standard: if `relationship order` never produces a concrete move, `relationship order` is not yet operational.
- User question: what is one thing `relationship order` would make me inspect differently right now?

### relationship order — What To Preserve

- Preserve the pressure: `relationship order` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `relationship order` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `relationship order`.
- Preserve the routing value: `relationship order` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `relationship order` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `relationship order` stops is part of preserving `relationship order` well.
- Preserve the usefulness: a transferred `relationship order` should still help someone make a cleaner judgment.

### relationship order — What To Drop

- Drop the costume: `relationship order` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `relationship order` has been understood.
- Drop the universal claim: `relationship order` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `relationship order` survives only as a catchphrase, `relationship order` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `relationship order` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `relationship order` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `relationship order` and remove the surrounding noise.

### relationship order — Design Implication

- Design implication: `relationship order` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `relationship order` that later edits can stay honest.
- UX implication: users should feel `relationship order` changing the angle of analysis, not just the tone.
- Evaluation implication: test `relationship order` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `relationship order` only if they sharpen the boundary of `relationship order` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `relationship order` should refuse.
- Bottom line: `relationship order` belongs in the repo only if `relationship order` keeps improving judgment after the historical costume is removed.


## Source Files

这些调研文件回链到本仓库 `references/sources/` 里的原始素材层。

- [`analects-ctext.md`](../sources/books/analects-ctext.md)
- [`doctrine-of-the-mean-ctext.md`](../sources/books/doctrine-of-the-mean-ctext.md)
- [`great-learning-ctext.md`](../sources/books/great-learning-ctext.md)
- [`britannica-confucius.md`](../sources/articles/britannica-confucius.md)
- [`sep-confucius.md`](../sources/articles/sep-confucius.md)

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[analects-ctext.md](../sources/books/analects-ctext.md)、[doctrine-of-the-mean-ctext.md](../sources/books/doctrine-of-the-mean-ctext.md)、[great-learning-ctext.md](../sources/books/great-learning-ctext.md)
- transcripts：（无）
- articles：[britannica-confucius.md](../sources/articles/britannica-confucius.md)、[sep-confucius.md](../sources/articles/sep-confucius.md)
