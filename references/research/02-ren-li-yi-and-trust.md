# 仁、礼、义与信 / Ren, Li, Yi, and Trust

Core ethical vocabulary around ren, li, yi, and trust as a social operating logic.

## Why This File Exists

- This file supports the Confucius person skill by making the research layer thick enough to guide real reasoning rather than surface style.
- It treats 孔子 as a structured cognitive system, not as a quotation source or prestige label.
- The focus of this file is Core ethical vocabulary around ren, li, yi, and trust as a social operating logic..
- The notes below are organized to keep historical grounding, operational transfer, and misuse boundaries visible at the same time.

## Source Orientation

- Read the primary texts or documentary anchors first, then map them to later interpretation.
- Keep biography, corpus boundary, and later myth separate from direct conceptual claims.
- When transferring this material into a modern skill, preserve the structure and drop the historical costume.

## Research Notes

### ren — Definition

- What this names: within `Core ethical vocabulary around ren, li, yi, and trust as a social operating logic`, `ren` points to a concrete node rather than a prestige label.
- Object boundary: treat `ren` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `ren` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `ren` before pulling it into a modern analogy.
- Skill value: `ren` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `ren` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `ren` clearly enough that a later answer can say what `ren` includes and excludes.

### ren — Historical Context

- Historical location: ask when `ren` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `ren` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `ren` says, but why `ren` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `ren` from later packaging or selective reuse.
- Reading move: map `ren` against the timeline before treating `ren` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `ren` before extracting any lesson from `ren`.
- Failure risk: without context, `ren` can look cleaner, more universal, and more innocent than it actually was.

### ren — Internal Tension

- Main tension: `ren` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `ren` makes possible and what `ren` quietly suppresses at the same time.
- Interpretive value: the tension in `ren` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `ren` double-sided rather than converting `ren` into moral reassurance.
- Research warning: if every sentence about `ren` sounds admiring or dismissive, `ren` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `ren`.
- Better question: what does `ren` solve, and what new silence does `ren` create?

### ren — Key Distinction

- Core distinction: separate `ren` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `ren`.
- Typical confusion: readers often mistake `ren` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `ren` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `ren`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `ren` by accident.
- Operational cue: define what `ren` is not before claiming to know what `ren` is.

### ren — Method

- Method question: how does `ren` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `ren` rather than inspirational phrasing.
- Research use: `ren` matters when `ren` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `ren`, not costume around `ren`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `ren`.
- Modern move: only transfer the mechanism in `ren` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `ren` without reproducing the method inside `ren`.

### ren — Operational Use

- Operational role: `ren` belongs in the skill because `ren` changes how prompts are framed and how problems are cut.
- Decision value: `ren` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `ren` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `ren` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `ren` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `ren` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `ren` make me check before I act?

### ren — Transfer To Modern Problems

- Transfer principle: bring `ren` forward only when the structural analogy around `ren` is stronger than the historical costume.
- Modern fit: `ren` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `ren` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `ren`, not the surrounding mythology around `ren`.
- Bad transfer: using `ren` to justify positions `ren` was never built to adjudicate.
- Practical test: if the analogy for `ren` cannot survive plainer language, `ren` probably does not belong in the answer.
- Design implication: every modern use of `ren` should make the analogy explicit instead of pretending `ren` is self-evident.

### ren — Failure Mode

- Failure pattern: `ren` goes bad when `ren` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `ren` for authority, mood, or aesthetic and lose the underlying discipline in `ren`.
- Research signal: when `ren` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `ren`.
- User risk: answers can start sounding impressive while `ren` becomes less falsifiable.
- Repository risk: `ren` can silently drift from operating concept into mascot.
- Correction move: pull `ren` back to evidence, mechanism, and concrete limits.
- Best warning label: `ren` should increase rigor, not lower the burden of proof.

### ren — Common Misreading

- Typical misreading: `ren` is often taken as a broad worldview when `ren` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `ren`.
- Consequence: readers then import claims into `ren` that the corpus does not actually support for `ren`.
- Skill response: keep `ren` attached to the texts, cases, and institutional situations that make `ren` legible.
- Modern correction: show what `ren` can illuminate without pretending `ren` answers everything nearby.
- Language fix: name the overreach around `ren` directly instead of just gesturing at nuance.
- Reader test: if `ren` suddenly explains every problem, `ren` is being misread.

### ren — Boundary Condition

- Boundary condition: `ren` only travels well when the receiving problem shares enough structure with the original one behind `ren`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `ren`.
- Ethical test: some uses of `ren` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `ren` into careless advice.
- Good limit: it is fine for `ren` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `ren` has no boundary because `ren` feels generically wise.
- Operational question: under what conditions should `ren` be left unused?

### ren — Better Question

- Better question: instead of asking whether `ren` is simply true, ask what problem `ren` was trying to solve.
- Reader upgrade: this turns `ren` from a label into a live diagnostic instrument.
- Research upgrade: `ren` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `ren` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `ren` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `ren` becomes useful when `ren` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `ren` is framed as a problem-solving move?

### ren — Example Move

- Example move: use `ren` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `ren` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `ren` is strongest when `ren` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `ren` lets the user do that they could not do before.
- Transfer discipline: keep the move around `ren` small and explicit so the analogy does not sprawl.
- Repository standard: if `ren` never produces a concrete move, `ren` is not yet operational.
- User question: what is one thing `ren` would make me inspect differently right now?

### ren — What To Preserve

- Preserve the pressure: `ren` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `ren` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `ren`.
- Preserve the routing value: `ren` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `ren` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `ren` stops is part of preserving `ren` well.
- Preserve the usefulness: a transferred `ren` should still help someone make a cleaner judgment.

### ren — What To Drop

- Drop the costume: `ren` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `ren` has been understood.
- Drop the universal claim: `ren` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `ren` survives only as a catchphrase, `ren` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `ren` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `ren` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `ren` and remove the surrounding noise.

### ren — Design Implication

- Design implication: `ren` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `ren` that later edits can stay honest.
- UX implication: users should feel `ren` changing the angle of analysis, not just the tone.
- Evaluation implication: test `ren` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `ren` only if they sharpen the boundary of `ren` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `ren` should refuse.
- Bottom line: `ren` belongs in the repo only if `ren` keeps improving judgment after the historical costume is removed.

### li — Definition

- What this names: within `Core ethical vocabulary around ren, li, yi, and trust as a social operating logic`, `li` points to a concrete node rather than a prestige label.
- Object boundary: treat `li` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `li` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `li` before pulling it into a modern analogy.
- Skill value: `li` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `li` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `li` clearly enough that a later answer can say what `li` includes and excludes.

### li — Historical Context

- Historical location: ask when `li` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `li` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `li` says, but why `li` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `li` from later packaging or selective reuse.
- Reading move: map `li` against the timeline before treating `li` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `li` before extracting any lesson from `li`.
- Failure risk: without context, `li` can look cleaner, more universal, and more innocent than it actually was.

### li — Internal Tension

- Main tension: `li` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `li` makes possible and what `li` quietly suppresses at the same time.
- Interpretive value: the tension in `li` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `li` double-sided rather than converting `li` into moral reassurance.
- Research warning: if every sentence about `li` sounds admiring or dismissive, `li` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `li`.
- Better question: what does `li` solve, and what new silence does `li` create?

### li — Key Distinction

- Core distinction: separate `li` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `li`.
- Typical confusion: readers often mistake `li` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `li` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `li`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `li` by accident.
- Operational cue: define what `li` is not before claiming to know what `li` is.

### li — Method

- Method question: how does `li` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `li` rather than inspirational phrasing.
- Research use: `li` matters when `li` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `li`, not costume around `li`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `li`.
- Modern move: only transfer the mechanism in `li` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `li` without reproducing the method inside `li`.

### li — Operational Use

- Operational role: `li` belongs in the skill because `li` changes how prompts are framed and how problems are cut.
- Decision value: `li` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `li` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `li` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `li` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `li` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `li` make me check before I act?

### li — Transfer To Modern Problems

- Transfer principle: bring `li` forward only when the structural analogy around `li` is stronger than the historical costume.
- Modern fit: `li` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `li` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `li`, not the surrounding mythology around `li`.
- Bad transfer: using `li` to justify positions `li` was never built to adjudicate.
- Practical test: if the analogy for `li` cannot survive plainer language, `li` probably does not belong in the answer.
- Design implication: every modern use of `li` should make the analogy explicit instead of pretending `li` is self-evident.

### li — Failure Mode

- Failure pattern: `li` goes bad when `li` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `li` for authority, mood, or aesthetic and lose the underlying discipline in `li`.
- Research signal: when `li` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `li`.
- User risk: answers can start sounding impressive while `li` becomes less falsifiable.
- Repository risk: `li` can silently drift from operating concept into mascot.
- Correction move: pull `li` back to evidence, mechanism, and concrete limits.
- Best warning label: `li` should increase rigor, not lower the burden of proof.

### li — Common Misreading

- Typical misreading: `li` is often taken as a broad worldview when `li` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `li`.
- Consequence: readers then import claims into `li` that the corpus does not actually support for `li`.
- Skill response: keep `li` attached to the texts, cases, and institutional situations that make `li` legible.
- Modern correction: show what `li` can illuminate without pretending `li` answers everything nearby.
- Language fix: name the overreach around `li` directly instead of just gesturing at nuance.
- Reader test: if `li` suddenly explains every problem, `li` is being misread.

### li — Boundary Condition

- Boundary condition: `li` only travels well when the receiving problem shares enough structure with the original one behind `li`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `li`.
- Ethical test: some uses of `li` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `li` into careless advice.
- Good limit: it is fine for `li` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `li` has no boundary because `li` feels generically wise.
- Operational question: under what conditions should `li` be left unused?

### li — Better Question

- Better question: instead of asking whether `li` is simply true, ask what problem `li` was trying to solve.
- Reader upgrade: this turns `li` from a label into a live diagnostic instrument.
- Research upgrade: `li` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `li` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `li` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `li` becomes useful when `li` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `li` is framed as a problem-solving move?

### li — Example Move

- Example move: use `li` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `li` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `li` is strongest when `li` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `li` lets the user do that they could not do before.
- Transfer discipline: keep the move around `li` small and explicit so the analogy does not sprawl.
- Repository standard: if `li` never produces a concrete move, `li` is not yet operational.
- User question: what is one thing `li` would make me inspect differently right now?

### li — What To Preserve

- Preserve the pressure: `li` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `li` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `li`.
- Preserve the routing value: `li` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `li` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `li` stops is part of preserving `li` well.
- Preserve the usefulness: a transferred `li` should still help someone make a cleaner judgment.

### li — What To Drop

- Drop the costume: `li` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `li` has been understood.
- Drop the universal claim: `li` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `li` survives only as a catchphrase, `li` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `li` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `li` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `li` and remove the surrounding noise.

### li — Design Implication

- Design implication: `li` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `li` that later edits can stay honest.
- UX implication: users should feel `li` changing the angle of analysis, not just the tone.
- Evaluation implication: test `li` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `li` only if they sharpen the boundary of `li` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `li` should refuse.
- Bottom line: `li` belongs in the repo only if `li` keeps improving judgment after the historical costume is removed.

### yi — Definition

- What this names: within `Core ethical vocabulary around ren, li, yi, and trust as a social operating logic`, `yi` points to a concrete node rather than a prestige label.
- Object boundary: treat `yi` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `yi` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `yi` before pulling it into a modern analogy.
- Skill value: `yi` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `yi` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `yi` clearly enough that a later answer can say what `yi` includes and excludes.

### yi — Historical Context

- Historical location: ask when `yi` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `yi` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `yi` says, but why `yi` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `yi` from later packaging or selective reuse.
- Reading move: map `yi` against the timeline before treating `yi` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `yi` before extracting any lesson from `yi`.
- Failure risk: without context, `yi` can look cleaner, more universal, and more innocent than it actually was.

### yi — Internal Tension

- Main tension: `yi` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `yi` makes possible and what `yi` quietly suppresses at the same time.
- Interpretive value: the tension in `yi` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `yi` double-sided rather than converting `yi` into moral reassurance.
- Research warning: if every sentence about `yi` sounds admiring or dismissive, `yi` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `yi`.
- Better question: what does `yi` solve, and what new silence does `yi` create?

### yi — Key Distinction

- Core distinction: separate `yi` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `yi`.
- Typical confusion: readers often mistake `yi` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `yi` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `yi`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `yi` by accident.
- Operational cue: define what `yi` is not before claiming to know what `yi` is.

### yi — Method

- Method question: how does `yi` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `yi` rather than inspirational phrasing.
- Research use: `yi` matters when `yi` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `yi`, not costume around `yi`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `yi`.
- Modern move: only transfer the mechanism in `yi` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `yi` without reproducing the method inside `yi`.

### yi — Operational Use

- Operational role: `yi` belongs in the skill because `yi` changes how prompts are framed and how problems are cut.
- Decision value: `yi` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `yi` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `yi` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `yi` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `yi` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `yi` make me check before I act?

### yi — Transfer To Modern Problems

- Transfer principle: bring `yi` forward only when the structural analogy around `yi` is stronger than the historical costume.
- Modern fit: `yi` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `yi` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `yi`, not the surrounding mythology around `yi`.
- Bad transfer: using `yi` to justify positions `yi` was never built to adjudicate.
- Practical test: if the analogy for `yi` cannot survive plainer language, `yi` probably does not belong in the answer.
- Design implication: every modern use of `yi` should make the analogy explicit instead of pretending `yi` is self-evident.

### yi — Failure Mode

- Failure pattern: `yi` goes bad when `yi` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `yi` for authority, mood, or aesthetic and lose the underlying discipline in `yi`.
- Research signal: when `yi` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `yi`.
- User risk: answers can start sounding impressive while `yi` becomes less falsifiable.
- Repository risk: `yi` can silently drift from operating concept into mascot.
- Correction move: pull `yi` back to evidence, mechanism, and concrete limits.
- Best warning label: `yi` should increase rigor, not lower the burden of proof.

### yi — Common Misreading

- Typical misreading: `yi` is often taken as a broad worldview when `yi` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `yi`.
- Consequence: readers then import claims into `yi` that the corpus does not actually support for `yi`.
- Skill response: keep `yi` attached to the texts, cases, and institutional situations that make `yi` legible.
- Modern correction: show what `yi` can illuminate without pretending `yi` answers everything nearby.
- Language fix: name the overreach around `yi` directly instead of just gesturing at nuance.
- Reader test: if `yi` suddenly explains every problem, `yi` is being misread.

### yi — Boundary Condition

- Boundary condition: `yi` only travels well when the receiving problem shares enough structure with the original one behind `yi`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `yi`.
- Ethical test: some uses of `yi` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `yi` into careless advice.
- Good limit: it is fine for `yi` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `yi` has no boundary because `yi` feels generically wise.
- Operational question: under what conditions should `yi` be left unused?

### yi — Better Question

- Better question: instead of asking whether `yi` is simply true, ask what problem `yi` was trying to solve.
- Reader upgrade: this turns `yi` from a label into a live diagnostic instrument.
- Research upgrade: `yi` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `yi` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `yi` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `yi` becomes useful when `yi` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `yi` is framed as a problem-solving move?

### yi — Example Move

- Example move: use `yi` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `yi` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `yi` is strongest when `yi` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `yi` lets the user do that they could not do before.
- Transfer discipline: keep the move around `yi` small and explicit so the analogy does not sprawl.
- Repository standard: if `yi` never produces a concrete move, `yi` is not yet operational.
- User question: what is one thing `yi` would make me inspect differently right now?

### yi — What To Preserve

- Preserve the pressure: `yi` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `yi` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `yi`.
- Preserve the routing value: `yi` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `yi` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `yi` stops is part of preserving `yi` well.
- Preserve the usefulness: a transferred `yi` should still help someone make a cleaner judgment.

### yi — What To Drop

- Drop the costume: `yi` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `yi` has been understood.
- Drop the universal claim: `yi` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `yi` survives only as a catchphrase, `yi` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `yi` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `yi` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `yi` and remove the surrounding noise.

### yi — Design Implication

- Design implication: `yi` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `yi` that later edits can stay honest.
- UX implication: users should feel `yi` changing the angle of analysis, not just the tone.
- Evaluation implication: test `yi` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `yi` only if they sharpen the boundary of `yi` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `yi` should refuse.
- Bottom line: `yi` belongs in the repo only if `yi` keeps improving judgment after the historical costume is removed.

### xin and trust — Definition

- What this names: within `Core ethical vocabulary around ren, li, yi, and trust as a social operating logic`, `xin and trust` points to a concrete node rather than a prestige label.
- Object boundary: treat `xin and trust` as a research handle with limits, not as a ready-made conclusion.
- Why it stays in the file: `xin and trust` changes how the figure is periodized, sourced, or operationalized.
- How to read it: start from the historical referent of `xin and trust` before pulling it into a modern analogy.
- Skill value: `xin and trust` gives the repo a sharper diagnostic vocabulary for judgment and transfer.
- Reader warning: when `xin and trust` is reduced to a slogan, the file loses the distinction it was built to protect.
- Practical use: define `xin and trust` clearly enough that a later answer can say what `xin and trust` includes and excludes.

### xin and trust — Historical Context

- Historical location: ask when `xin and trust` first becomes visible in the surviving record and through which institution, text, or episode.
- Surrounding pressure: `xin and trust` usually sits inside a wider dispute about authority, method, legitimacy, or transmission.
- Context clue: the point is not only what `xin and trust` says, but why `xin and trust` mattered in its own setting.
- Corpus implication: historical placement helps distinguish stable doctrine around `xin and trust` from later packaging or selective reuse.
- Reading move: map `xin and trust` against the timeline before treating `xin and trust` as timeless method.
- Transfer discipline: preserve the contextual problem that produced `xin and trust` before extracting any lesson from `xin and trust`.
- Failure risk: without context, `xin and trust` can look cleaner, more universal, and more innocent than it actually was.

### xin and trust — Internal Tension

- Main tension: `xin and trust` usually bundles a capability gain with a distortion cost instead of offering one clean virtue.
- Diagnostic split: ask what `xin and trust` makes possible and what `xin and trust` quietly suppresses at the same time.
- Interpretive value: the tension in `xin and trust` matters because this repo is meant to preserve pressure, not just preference.
- Operational lesson: strong skills keep `xin and trust` double-sided rather than converting `xin and trust` into moral reassurance.
- Research warning: if every sentence about `xin and trust` sounds admiring or dismissive, `xin and trust` has already been flattened.
- Transfer rule: modern answers should name both the leverage and the blind spot living inside `xin and trust`.
- Better question: what does `xin and trust` solve, and what new silence does `xin and trust` create?

### xin and trust — Key Distinction

- Core distinction: separate `xin and trust` from adjacent ideas that look similar but solve a different problem.
- Why this matters: a person skill only becomes precise when near-miss concepts stop leaking into `xin and trust`.
- Typical confusion: readers often mistake `xin and trust` for style, morality, charisma, or doctrine in general.
- Sharper read: treat `xin and trust` as one specific lever inside the wider system rather than the whole system.
- Corpus benefit: this distinction helps decide which quotes, cases, or texts actually belong under `xin and trust`.
- Transfer benefit: it keeps modern analogies from importing too much historical baggage through `xin and trust` by accident.
- Operational cue: define what `xin and trust` is not before claiming to know what `xin and trust` is.

### xin and trust — Method

- Method question: how does `xin and trust` actually work in texts, institutions, habits, or decisions rather than in rhetoric alone?
- Process lens: look for sequence, mechanism, routing, or recurrence inside `xin and trust` rather than inspirational phrasing.
- Research use: `xin and trust` matters when `xin and trust` changes how attention, evidence, or action gets organized.
- Practical inference: the skill should borrow method from `xin and trust`, not costume around `xin and trust`.
- Good reading habit: ask which inputs, filters, and outputs are being structured by `xin and trust`.
- Modern move: only transfer the mechanism in `xin and trust` if you can name the real-world process it still corresponds to.
- Bad move: repeating the vocabulary of `xin and trust` without reproducing the method inside `xin and trust`.

### xin and trust — Operational Use

- Operational role: `xin and trust` belongs in the skill because `xin and trust` changes how prompts are framed and how problems are cut.
- Decision value: `xin and trust` sharpens what to inspect first, what to distrust, and what counts as progress.
- Prompt value: when `xin and trust` is active, answers should become more selective about evidence and boundary conditions.
- Design value: `xin and trust` can route users toward the right frame instead of merely giving them better wording.
- Testing value: `xin and trust` suggests what kind of real example would confirm or falsify the current interpretation.
- Usage boundary: `xin and trust` should guide reasoning, not become an ornamental tag glued to every response.
- Operator question: what does `xin and trust` make me check before I act?

### xin and trust — Transfer To Modern Problems

- Transfer principle: bring `xin and trust` forward only when the structural analogy around `xin and trust` is stronger than the historical costume.
- Modern fit: `xin and trust` often transfers best into questions about decision quality, perception, incentives, coordination, or interpretation.
- Boundary line: do not smuggle era-specific authority relations from `xin and trust` into contemporary settings that lack the same structure.
- Good transfer: extract the problem-solving intelligence in `xin and trust`, not the surrounding mythology around `xin and trust`.
- Bad transfer: using `xin and trust` to justify positions `xin and trust` was never built to adjudicate.
- Practical test: if the analogy for `xin and trust` cannot survive plainer language, `xin and trust` probably does not belong in the answer.
- Design implication: every modern use of `xin and trust` should make the analogy explicit instead of pretending `xin and trust` is self-evident.

### xin and trust — Failure Mode

- Failure pattern: `xin and trust` goes bad when `xin and trust` becomes a shortcut to certainty rather than a pressure test.
- Common collapse: people borrow `xin and trust` for authority, mood, or aesthetic and lose the underlying discipline in `xin and trust`.
- Research signal: when `xin and trust` is invoked too quickly, nuance disappears and contradiction gets cleaned out of `xin and trust`.
- User risk: answers can start sounding impressive while `xin and trust` becomes less falsifiable.
- Repository risk: `xin and trust` can silently drift from operating concept into mascot.
- Correction move: pull `xin and trust` back to evidence, mechanism, and concrete limits.
- Best warning label: `xin and trust` should increase rigor, not lower the burden of proof.

### xin and trust — Common Misreading

- Typical misreading: `xin and trust` is often taken as a broad worldview when `xin and trust` is actually one bounded instrument inside the larger system.
- Why this happens: selective quotation and later myth-making reward the simplest version of `xin and trust`.
- Consequence: readers then import claims into `xin and trust` that the corpus does not actually support for `xin and trust`.
- Skill response: keep `xin and trust` attached to the texts, cases, and institutional situations that make `xin and trust` legible.
- Modern correction: show what `xin and trust` can illuminate without pretending `xin and trust` answers everything nearby.
- Language fix: name the overreach around `xin and trust` directly instead of just gesturing at nuance.
- Reader test: if `xin and trust` suddenly explains every problem, `xin and trust` is being misread.

### xin and trust — Boundary Condition

- Boundary condition: `xin and trust` only travels well when the receiving problem shares enough structure with the original one behind `xin and trust`.
- Context test: check scale, authority, evidence quality, and institutional shape before borrowing `xin and trust`.
- Ethical test: some uses of `xin and trust` cross from interpretation into endorsement, coercion, or myth-making.
- Skill discipline: boundary conditions stop the repo from turning `xin and trust` into careless advice.
- Good limit: it is fine for `xin and trust` to illuminate a narrow slice of the problem instead of the whole field.
- Bad limit: pretending `xin and trust` has no boundary because `xin and trust` feels generically wise.
- Operational question: under what conditions should `xin and trust` be left unused?

### xin and trust — Better Question

- Better question: instead of asking whether `xin and trust` is simply true, ask what problem `xin and trust` was trying to solve.
- Reader upgrade: this turns `xin and trust` from a label into a live diagnostic instrument.
- Research upgrade: `xin and trust` forces the file to connect claims to situations, stakes, and tradeoffs.
- Prompt upgrade: `xin and trust` helps users formulate sharper questions about mechanism, motive, and transfer.
- Interpretive upgrade: `xin and trust` keeps the answer from freezing into admiration or rejection too early.
- Skill upgrade: `xin and trust` becomes useful when `xin and trust` generates better questions faster than slogans.
- Best follow-up: what becomes newly visible once `xin and trust` is framed as a problem-solving move?

### xin and trust — Example Move

- Example move: use `xin and trust` to reframe a messy issue into a smaller set of pressure points you can actually inspect.
- Practical scene: a good answer might use `xin and trust` to separate motive from appearance, structure from story, or method from mythology.
- Why this works: `xin and trust` is strongest when `xin and trust` clarifies a live decision rather than decorating a summary.
- Demonstration value: an example move shows what `xin and trust` lets the user do that they could not do before.
- Transfer discipline: keep the move around `xin and trust` small and explicit so the analogy does not sprawl.
- Repository standard: if `xin and trust` never produces a concrete move, `xin and trust` is not yet operational.
- User question: what is one thing `xin and trust` would make me inspect differently right now?

### xin and trust — What To Preserve

- Preserve the pressure: `xin and trust` should keep its original demand for discrimination, not just its vocabulary.
- Preserve the asymmetry: retain what `xin and trust` notices earlier or more sharply than neighboring frames.
- Preserve the cost awareness: keep visible the tradeoff or risk that comes bundled with `xin and trust`.
- Preserve the routing value: `xin and trust` should still change what kind of evidence or example gets loaded first.
- Preserve the specificity: tie `xin and trust` to actual texts, episodes, or institutional realities whenever possible.
- Preserve the limit: knowing where `xin and trust` stops is part of preserving `xin and trust` well.
- Preserve the usefulness: a transferred `xin and trust` should still help someone make a cleaner judgment.

### xin and trust — What To Drop

- Drop the costume: `xin and trust` does not need borrowed historical atmosphere to remain useful.
- Drop the hero worship: admiration is not evidence that `xin and trust` has been understood.
- Drop the universal claim: `xin and trust` should not be stretched into a master key for unrelated problems.
- Drop the empty slogan: if `xin and trust` survives only as a catchphrase, `xin and trust` should be cut back to mechanism.
- Drop the false confidence: uncertainty about `xin and trust` is often healthier than inflated clarity.
- Drop the prestige borrowing: do not let `xin and trust` stand in for argument, sourcing, or proof.
- Drop the excess packaging: keep the operational core of `xin and trust` and remove the surrounding noise.

### xin and trust — Design Implication

- Design implication: `xin and trust` should influence routing, prompt phrasing, and evidence selection inside the final skill.
- Repository implication: the file should store enough structure around `xin and trust` that later edits can stay honest.
- UX implication: users should feel `xin and trust` changing the angle of analysis, not just the tone.
- Evaluation implication: test `xin and trust` on cases where naive admiration or rejection would both fail.
- Maintenance implication: future additions should land under `xin and trust` only if they sharpen the boundary of `xin and trust` rather than blur it.
- Safety implication: the skill must say what kinds of modern uses `xin and trust` should refuse.
- Bottom line: `xin and trust` belongs in the repo only if `xin and trust` keeps improving judgment after the historical costume is removed.


## Source Files

这些调研文件回链到本仓库 `references/sources/` 里的原始素材层。

- [`analects-ctext.md`](../sources/books/analects-ctext.md)
- [`doctrine-of-the-mean-ctext.md`](../sources/books/doctrine-of-the-mean-ctext.md)
- [`great-learning-ctext.md`](../sources/books/great-learning-ctext.md)
- [`britannica-confucius.md`](../sources/articles/britannica-confucius.md)
- [`sep-confucius.md`](../sources/articles/sep-confucius.md)

## 原始素材文件

这份 research 直接回链到本仓库的原始素材层：

- books：[analects-ctext.md](../sources/books/analects-ctext.md)、[doctrine-of-the-mean-ctext.md](../sources/books/doctrine-of-the-mean-ctext.md)、[great-learning-ctext.md](../sources/books/great-learning-ctext.md)
- transcripts：（无）
- articles：[britannica-confucius.md](../sources/articles/britannica-confucius.md)、[sep-confucius.md](../sources/articles/sep-confucius.md)
